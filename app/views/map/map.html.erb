<canvas id="mapCanvas" width="1000" height="500"></canvas>
<script>
  window.hsMapData = {
    events: <%=h @events.to_json(methods: :descriptions, include: :event_participants).html_safe %>,
    spaceTimes: <%=h @space_times.to_json.html_safe %>,
    things: <%=h @things.to_json(include: :instances).html_safe %>,
    containers: <%=h @containers.to_json.html_safe %>
  };
</script>
